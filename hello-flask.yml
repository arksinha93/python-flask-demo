---
- name: Hello Flask
  hosts: 20.198.73.9
  become: true
  
  tasks:
    - name: Install docker
      yum:
        name:
          - docker
          - git
        state: present
        update_cache: yes
 
    - name: Enable and Start Docker service
      service:
        name: docker
        enabled: yes
        state: started
